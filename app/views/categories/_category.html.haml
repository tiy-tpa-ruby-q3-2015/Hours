%li
  .project
    %span.color{:style => "background-color:#{category.name.pastel_color};"}
    %span= category.name
    %span
      #{link_to t("category.edit"), edit_category_path(category)}
      | #{link_to t("category.destroy"), category_path(category), method: :delete}
